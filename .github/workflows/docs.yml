name: Docs

on:
  push:
    branches:
    - main
    - 'releases/**'
  pull_request:

jobs:
  build:
    runs-on: 'ubuntu-latest'
    
    env:
      BUILD_TYPE: RELEASE

    steps:
    - uses: actions/checkout@v2

    - name: apt-get
      run: |
        sudo apt-get install doxygen

    - name: pip
      run: |
        sudo pip3 install -r docs/requirements.txt

    - name: Sphinx
      run: sphinx-build ${{ github.workspace }}/docs ${{ github.workspace }}/build/docs

    - name: Artifact
      uses: actions/upload-artifact@v2
      with:
        name: docs
        path: build/docs
